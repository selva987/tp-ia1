/**
 * Clase "abstracta" que sirve de base para todas las clases que se muestren en un grafico
 */

class ElementoGrafico {
    constructor(id, label) {
        this.id = id;
        this.label = label;
    }
}